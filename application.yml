eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost
    
spring:
    datasource:
        driver-class-name: com.mysql.jdbc.Driver
        password: ThePassword
        url: jdbc:mysql://10.76.136.211:3306/db_example
        username: springuser
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true    
    
